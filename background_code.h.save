#ifndef BACKGROUND_CODE_H_INCLUDED
#define BACKGROUND_CODE_H_INCLUDED

string intToStr(int n);

string compressRLE(string user_input, bool is_test);

string decompressRLE(string compressed, bool is_test);

string compressBYTERUN(string user_input, bool is_test);

string decompressBYTERUN(string compressed, bool is_test);


#endif // BACKGROUND_CODE_H_INCLUDED
